spring:
  application:
    name: auth
  config:
    activate:
      on-profile: local
    import: aws-parameterstore:/config/auth_local/
  cloud:
    function:
      definition: lambdaEventListener
logging:
  level:
    root: INFO
jwt:
  public_key: ${jwt_public_key}